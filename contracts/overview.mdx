---
title: "Overview"
description: "Introduction to x402r smart contracts and their relationship to commerce-payments"
icon: "circle-info"
---

## What is x402r?

x402r is a smart contract extension for HTTP-native refundable payments. It builds on [commerce-payments](https://github.com/BackTrackCo/commerce-payments) to add dispute resolution, escrow periods, and flexible refund capabilities.

## Architecture Layers

See the [system architecture diagrams](https://github.com/BackTrackCo/x402r-contracts#architecture) in the x402r-contracts repository.

## Commerce Payments (Base Layer)

[Commerce Payments](https://github.com/BackTrackCo/commerce-payments) provides the foundational payment infrastructure.

<Note>
x402r uses a [fork of commerce-payments](https://github.com/BackTrackCo/commerce-payments) that adds **partial void** support for handling partially completed orders and partial refunds.
</Note>

### AuthCaptureEscrow

Core escrow contract for holding ERC-20 tokens during payments.

**Features:**
- Authorization-based deposits (no direct transfers)
- Payment state machine: `NonExistent` → `InEscrow` → `Released` → `Settled`
- Void/reclaim for failed authorizations
- **Partial void support** (x402r addition) - Refund partial amounts during escrow
- Operator-based access control

**Key Methods:**
```solidity
authorize(paymentId, payer, receiver, amount, token, operator)
charge(paymentId, amount)
release(paymentId)
void(paymentId)
reclaim(paymentId, receiver, amount)
```

### ERC3009PaymentCollector

Payment collection with ERC-3009 transferWithAuthorization support.

**Features:**
- Gasless payments via meta-transactions
- Nonce-based replay protection
- Deadline-based expiry
- Integration with Multicall3 for batching

### TokenStore

Safe token storage and transfer utilities.

**Features:**
- Reentrancy-safe transfers
- Balance tracking
- Integration with OpenZeppelin SafeERC20

## What x402r Adds

x402r extends commerce-payments with refund-specific capabilities:

### 1. Arbitration & Dispute Resolution

**ArbitrationOperator** - Payment operator with designated arbiter for disputes

**RefundRequest** - Structured refund request workflow

**Key additions:**
- Arbiter role for neutral third-party dispute resolution
- Refund request states: `Pending` → `Approved`/`Denied`/`Cancelled`
- In-escrow refunds (during escrow period)
- Post-escrow refunds (after release)
- Fee distribution to protocol and arbiters

### 2. Pluggable Condition System

**Conditions (ICondition)** - Authorization checks before actions

**Recorders (IRecorder)** - State updates after actions

**10-slot configuration per operator:**
- 5 condition slots (before action): authorize, charge, release, refundInEscrow, refundPostEscrow
- 5 recorder slots (after action): state tracking for each action

**Benefits:**
- Configure operator behavior without redeploying
- Compose complex authorization logic with combinators
- Reuse condition contracts across operators
- Gas-efficient with shared singletons

### 3. Time-Based Escrow & Freeze Policies

**EscrowPeriodCondition** - Checks if escrow period has elapsed

**EscrowPeriodRecorder** - Tracks authorization time and freeze state

**FreezePolicy** - Defines who can freeze/unfreeze and for how long

**Key features:**
- Configurable escrow periods (e.g., 7 days, 14 days)
- Payer-initiated freezes to stop suspicious releases
- Time-limited freeze durations (e.g., 3 days)
- MEV protection via private mempool support

### 4. Factory Pattern

**ArbitrationOperatorFactory** - Deploys operators with deterministic CREATE2 addresses

**EscrowPeriodConditionFactory** - Deploys condition/recorder pairs

**FreezePolicyFactory** - Deploys freeze policy instances

**Benefits:**
- Predictable addresses for off-chain address generation
- Shared configuration reduces deployment costs
- Idempotent deployments (same config = same address)
- Owner controls all deployed instances via factory

## Key Differences from Commerce Payments

| Feature | Commerce Payments | x402r |
|---------|------------------|-------|
| **Refunds** | Manual void/reclaim | Structured refund requests with arbiter approval |
| **Escrow Period** | Not enforced | Configurable time-lock before release |
| **Dispute Resolution** | Not built-in | Arbiter role with decision workflow |
| **Authorization** | Operator-based only | Pluggable conditions (access, time, combinators) |
| **Freeze Mechanism** | Not available | Payer can freeze during escrow period |
| **Deployment** | Direct deployment | Factory pattern with CREATE2 |
| **Fees** | Not enforced | Configurable protocol and arbiter fees |

## Use Cases

### Commerce Payments (Base) Suitable For:
- Simple payment escrow
- Trusted operator scenarios
- No refund requirements
- Direct integrations

### x402r (Extension) Suitable For:
- Marketplace payments with buyer protection
- Escrow with time-based release
- Disputed transactions requiring arbitration
- Complex authorization requirements
- Freeze protection against fraud

## Design Principles

<Accordion title="Immutability by Default">
Most contracts are immutable to prevent rug pulls and ensure trustlessness:
- **ArbitrationOperator** - Cannot pause or upgrade
- **EscrowPeriodCondition** - Cannot modify escrow period
- **FreezePolicy** - Cannot change freeze rules

Only fee configuration is mutable (with 24-hour timelock).
</Accordion>

<Accordion title="Gas Optimization">
- Condition singletons deployed once, reused everywhere
- CREATE2 for deterministic addresses (no registry lookups)
- Minimal storage in operators (conditions are stateless)
- Recorder pattern separates state from logic
</Accordion>

<Accordion title="Composability">
- Conditions can be combined with And/Or/Not logic
- Operators can share condition implementations
- Factories enable on-demand instance deployment
- Stateless conditions work across multiple operators
</Accordion>

<Accordion title="Security First">
- Reentrancy guards on all state changes
- 24-hour timelock on fee changes
- Two-step ownership transfers
- Comprehensive event logging for monitoring
</Accordion>

## Architecture Overview

For a detailed view of how all contracts interact, see [Architecture](/contracts/architecture).

To understand individual contracts, see [Core Contracts](/contracts/core-contracts).

For factory deployment patterns, see [Factories](/contracts/factories).

## Next Steps

<CardGroup cols={2}>
  <Card title="Architecture" icon="diagram-project" href="/contracts/architecture">
    View system architecture diagrams and payment flows.
  </Card>
  <Card title="Core Contracts" icon="file-contract" href="/contracts/core-contracts">
    Learn about ArbitrationOperator, RefundRequest, and other core contracts.
  </Card>
  <Card title="Factories" icon="industry" href="/contracts/factories">
    Understand factory patterns and CREATE2 deployments.
  </Card>
  <Card title="Examples" icon="code" href="/contracts/examples">
    See configuration examples for common use cases.
  </Card>
</CardGroup>
