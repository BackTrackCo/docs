---
title: "Conditions & Recorders"
description: "Pluggable condition system, combinators, and state recorders"
icon: "filter"
---

## Overview

Conditions and recorders enable flexible authorization and state tracking without modifying operator contracts. They follow simple interfaces that can be composed and reused.

## Condition System

### ICondition Interface

```solidity
interface ICondition {
    function check(
        PaymentInfo calldata payment,
        address caller
    ) external view returns (bool);
}
```

**Purpose:** Return `true` if the caller is authorized to perform an action, `false` otherwise.

**When Called:** Before operator actions (authorize, charge, release, refund)

**Example:**
```solidity
// Check if caller is the payment receiver
function check(PaymentInfo calldata payment, address caller)
    external view returns (bool)
{
    return caller == payment.receiver;
}
```

### IRecorder Interface

```solidity
interface IRecorder {
    function record(
        PaymentInfo calldata payment,
        uint256 amount,
        address caller
    ) external;
}
```

**Purpose:** Update state after an action successfully executes.

**When Called:** After operator actions complete

**Example:**
```solidity
// Record authorization timestamp
function record(PaymentInfo calldata payment, uint256 amount, address caller)
    external
{
    authorizedAt[payment.paymentId] = uint40(block.timestamp);
}
```

### Default Behavior

**Condition slot = `address(0)`**
- Always returns `true` (allow)
- Action is always authorized

**Recorder slot = `address(0)`**
- No-op (does nothing)
- No state is recorded

---

## Access Conditions

Pre-deployed singletons for role-based access control.

### PayerCondition

Only the payment payer can call the action.

**Address (Base Sepolia):** `0xDc0D800007ceACFf1299b926Ce22B4d4edCE6Ce7`

**Logic:**
```solidity
function check(PaymentInfo calldata payment, address caller)
    external pure returns (bool)
{
    return caller == payment.payer;
}
```

**Use cases:**
- Authorize: Let payer create payments
- Freeze: Let payer freeze suspicious payments
- Refund: Let payer request refunds

### ReceiverCondition

Only the payment receiver can call the action.

**Address (Base Sepolia):** `0x138Bf828643350AA3692aedDE8b2254eDF4D07EF`

**Logic:**
```solidity
function check(PaymentInfo calldata payment, address caller)
    external pure returns (bool)
{
    return caller == payment.receiver;
}
```

**Use cases:**
- Release: Let receiver release funds after escrow
- Charge: Let receiver charge partial amounts
- Refund: Let receiver voluntarily refund

### ArbiterCondition

Only the designated arbiter can call the action.

**Address (Base Sepolia):** `0x32471D31910a009273A812dE0894d9f0ADef4834`

**Logic:**
```solidity
function check(PaymentInfo calldata payment, address caller)
    external view returns (bool)
{
    return caller == operator.ARBITER();
}
```

**Use cases:**
- Refund: Let arbiter resolve disputes
- Release: Let arbiter force release if needed
- Custom: Arbiter-controlled flows

### AlwaysTrueCondition

Anyone can call the action (no restrictions).

**Address (Base Sepolia):** `0xe2659dc0d716B1226DF6a09A5f47862cd1ff6733`

**Logic:**
```solidity
function check(PaymentInfo calldata payment, address caller)
    external pure returns (bool)
{
    return true;
}
```

**Use cases:**
- Authorize: Let anyone create payments
- Open actions: Public function access

---

## Combinator Conditions

Compose multiple conditions with logical operators.

### AndCondition

All conditions must pass (`A && B && C`).

**Usage:**
```solidity
// Deploy combinator
AndCondition combo = new AndCondition([
    RECEIVER_CONDITION,          // Must be receiver
    ESCROW_PERIOD_CONDITION      // Must be after escrow
]);

// Use in operator config
config.releaseCondition = address(combo);
```

**Example:** Release requires receiver AND escrow passed

### OrCondition

At least one condition must pass (`A || B`).

**Usage:**
```solidity
// Receiver OR Arbiter can release
OrCondition combo = new OrCondition([
    RECEIVER_CONDITION,
    ARBITER_CONDITION
]);

config.releaseCondition = address(combo);
```

**Example:** Either receiver or arbiter can release

### NotCondition

Inverts a condition (`!A`).

**Usage:**
```solidity
// Anyone EXCEPT payer can call
NotCondition combo = new NotCondition(PAYER_CONDITION);

config.releaseCondition = address(combo);
```

**Example:** Prevent payer from releasing their own payment

### Nested Combinators

Combine combinators for complex logic:

```solidity
// (Receiver OR Arbiter) AND EscrowPassed
OrCondition receiverOrArbiter = new OrCondition([
    RECEIVER_CONDITION,
    ARBITER_CONDITION
]);

AndCondition releaseCondition = new AndCondition([
    address(receiverOrArbiter),
    ESCROW_PERIOD_CONDITION
]);

config.releaseCondition = address(releaseCondition);
```

**Logic Tree:**
```
AndCondition
├─ OrCondition
│  ├─ ReceiverCondition ✓
│  └─ ArbiterCondition ✗
└─ EscrowPeriodCondition ✓
Result: PASS (one branch of OR passed, AND both passed)
```

---

## Time-Based Conditions

### EscrowPeriodCondition

Checks if escrow period has elapsed and payment is not frozen.

**Deployment:** Via EscrowPeriodConditionFactory

**Logic:**
```solidity
function check(PaymentInfo calldata payment, address caller)
    external view returns (bool)
{
    RecordedInfo memory info = recorder.getInfo(payment.paymentId);

    // Must be authorized
    if (info.authorizedAt == 0) return false;

    // Must not be frozen
    if (info.frozen) return false;

    // Escrow period must have passed
    return block.timestamp >= info.authorizedAt + escrowPeriod;
}
```

**Checks:**
1. Payment was authorized (has timestamp)
2. Payment is not currently frozen
3. Current time ≥ authorization time + escrow period

**Use cases:**
- Time-lock releases (7-day escrow)
- Delayed fund access
- Grace periods before actions

### Freeze Policy

Determines who can freeze/unfreeze and for how long.

**Deployment:** Via FreezePolicyFactory

**Methods:**
```solidity
function canFreeze(PaymentInfo calldata payment, address caller)
    external view returns (bool);

function canUnfreeze(PaymentInfo calldata payment, address caller)
    external view returns (bool);

function isFrozen(uint40 frozenAt)
    external view returns (bool);
```

**Example Configuration:**
```typescript
// Payer can freeze for 3 days
const freezePolicy = await factory.deploy(
  PAYER_CONDITION,      // freeze condition
  PAYER_CONDITION,      // unfreeze condition
  3 * 24 * 60 * 60      // 3 days
);
```

**Freeze Duration:**
- Payment frozen at time `T`
- Freeze expires at `T + freezeDuration`
- After expiry, payment automatically unfrozen
- Can be manually unfrozen earlier if `unfreezeCondition` allows

---

## Recorders

### EscrowPeriodRecorder

Records authorization time and freeze state for time-based conditions.

**Deployment:** Via EscrowPeriodConditionFactory (paired with EscrowPeriodCondition)

**State:**
```solidity
struct RecordedInfo {
    uint40 authorizedAt;  // When payment was authorized
    bool frozen;          // Is payment currently frozen
    uint40 frozenAt;      // When payment was frozen
}

mapping(bytes32 => RecordedInfo) public recordedInfo;
```

**Methods:**
```solidity
// Called after authorize()
function record(PaymentInfo calldata payment, uint256 amount, address caller)
    external
{
    recordedInfo[payment.paymentId].authorizedAt = uint40(block.timestamp);
}

// Freeze payment
function freeze(bytes32 paymentId, PaymentInfo calldata payment)
    external
{
    require(freezePolicy.canFreeze(payment, msg.sender));
    recordedInfo[paymentId].frozen = true;
    recordedInfo[paymentId].frozenAt = uint40(block.timestamp);
}

// Unfreeze payment
function unfreeze(bytes32 paymentId, PaymentInfo calldata payment)
    external
{
    require(freezePolicy.canUnfreeze(payment, msg.sender));
    recordedInfo[paymentId].frozen = false;
}
```

**Usage:**
1. Pair with EscrowPeriodCondition
2. Set as `authorizeRecorder` and `releaseRecorder`
3. Condition reads state from recorder

### Custom Recorders

You can create custom recorders for specialized tracking:

```solidity
contract ReleaseCountRecorder is IRecorder {
    mapping(bytes32 => uint256) public releaseCount;
    mapping(bytes32 => uint256) public totalReleased;

    function record(
        PaymentInfo calldata payment,
        uint256 amount,
        address caller
    ) external {
        releaseCount[payment.paymentId]++;
        totalReleased[payment.paymentId] += amount;
    }

    // Enable tracking partial releases
    function getStats(bytes32 paymentId)
        external view
        returns (uint256 count, uint256 total)
    {
        return (releaseCount[paymentId], totalReleased[paymentId]);
    }
}
```

---

## Configuration Patterns

### Pattern 1: Open Authorization, Restricted Release

```solidity
config = {
    authorizeCondition: ALWAYS_TRUE_CONDITION,  // Anyone can authorize
    authorizeRecorder: escrowRecorder,          // Record time
    // ...
    releaseCondition: releaseCondition,         // Restricted
    releaseRecorder: escrowRecorder,
    // ...
};
```

**Use case:** Public payment creation, but controlled release

### Pattern 2: Payer-Only Actions

```solidity
config = {
    authorizeCondition: PAYER_CONDITION,        // Only payer
    authorizeRecorder: address(0),              // No recording
    // ...
    releaseCondition: PAYER_CONDITION,          // Only payer
    releaseRecorder: address(0),
    // ...
};
```

**Use case:** Self-service payment system

### Pattern 3: Arbiter-Controlled Everything

```solidity
config = {
    authorizeCondition: ARBITER_CONDITION,      // Only arbiter
    authorizeRecorder: address(0),
    chargeCondition: ARBITER_CONDITION,         // Only arbiter
    chargeRecorder: address(0),
    releaseCondition: ARBITER_CONDITION,        // Only arbiter
    releaseRecorder: address(0),
    refundInEscrowCondition: ARBITER_CONDITION, // Only arbiter
    refundInEscrowRecorder: address(0),
    refundPostEscrowCondition: ARBITER_CONDITION,
    refundPostEscrowRecorder: address(0)
};
```

**Use case:** Fully managed escrow service

### Pattern 4: Multi-Party Release

```solidity
// Requires 2 of 3: Payer, Receiver, Arbiter
const twoOfThree = new OrCondition([
    new AndCondition([PAYER_CONDITION, RECEIVER_CONDITION]),
    new AndCondition([PAYER_CONDITION, ARBITER_CONDITION]),
    new AndCondition([RECEIVER_CONDITION, ARBITER_CONDITION])
]);

config.releaseCondition = address(twoOfThree);
```

**Use case:** Multi-sig style releases

---

## Gas Optimization

### Singleton Reuse

Deploy conditions once, reuse everywhere:

```typescript
// ✅ Good: Reuse singleton
const config1 = { authorizeCondition: PAYER_CONDITION };
const config2 = { authorizeCondition: PAYER_CONDITION }; // Same instance

// ❌ Bad: Deploy multiple times
const payer1 = await new PayerCondition();
const payer2 = await new PayerCondition(); // Wasteful
```

### Combinator Efficiency

Simpler combinators = less gas:

```typescript
// ✅ Better: 2 conditions
OrCondition([A, B])  // ~25K gas per check

// ❌ Worse: 4 conditions
OrCondition([A, B, C, D])  // ~45K gas per check
```

### Stateless Conditions

Prefer stateless conditions when possible:

```solidity
// ✅ Stateless: No storage reads
function check(PaymentInfo calldata payment, address caller)
    external pure returns (bool)
{
    return caller == payment.receiver;  // Pure computation
}

// ❌ Stateful: Storage reads
function check(PaymentInfo calldata payment, address caller)
    external view returns (bool)
{
    return allowList[caller];  // SLOAD costs gas
}
```

---

## Security Considerations

<Warning>
**Condition Reentrancy:** Conditions should be `view` or `pure` to prevent reentrancy attacks. Never make external calls in conditions.
</Warning>

<Warning>
**Recorder State:** Recorders modify state. Ensure they have proper access control and cannot be called by unauthorized addresses.
</Warning>

<Tip>
**Test Thoroughly:** Custom conditions/recorders should have comprehensive test coverage. Edge cases in authorization logic can lead to locked funds.
</Tip>

---

## Advanced: Custom Conditions

Create custom conditions for specialized logic:

```solidity
contract TimeOfDayCondition is ICondition {
    uint256 public immutable startHour;  // e.g., 9 (9 AM)
    uint256 public immutable endHour;    // e.g., 17 (5 PM)

    constructor(uint256 _startHour, uint256 _endHour) {
        startHour = _startHour;
        endHour = _endHour;
    }

    function check(
        PaymentInfo calldata payment,
        address caller
    ) external view returns (bool) {
        uint256 hour = (block.timestamp / 3600) % 24;
        return hour >= startHour && hour < endHour;
    }
}

// Usage: Only allow releases during business hours
const businessHours = await new TimeOfDayCondition(9, 17);
config.releaseCondition = businessHours.address;
```

## Next Steps

<CardGroup cols={2}>
  <Card title="Examples" icon="code" href="/contracts/examples">
    See complete configuration examples for common use cases.
  </Card>
  <Card title="Deploy Contracts" icon="rocket" href="/contract-deployment">
    Deploy contracts with your custom configurations.
  </Card>
  <Card title="Factories" icon="industry" href="/contracts/factories">
    Learn how to deploy condition instances.
  </Card>
</CardGroup>
