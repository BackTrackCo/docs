{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); case \"$f\" in *.mdx) command -v vale >/dev/null 2>&1 && (cd \"$CLAUDE_PROJECT_DIR/docs\" && vale \"$f\" 2>&1) || true ;; esac"
          }
        ]
      }
    ]
  }
}
